# Web

This folder is for generated local dashboards.

Public-safe usage:

- commit only generic dashboard templates or screenshots built from fake data
- do not commit dashboards generated from real personal memory or real logged-in social feeds

Starter command:

```bash
python3 scripts/build_demo_dashboard.py
```
