# Automation

This folder is for local runtime state and scheduler notes.

In a public repository, keep only templates and documentation here.

Do not commit:

- live scheduler state
- logs
- browser profiles
- cookies or session data
- machine-specific `plist` files unless they are sanitized examples
