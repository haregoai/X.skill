[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-wiki-open"
version = "0.1.0"
description = "Public scaffold for a file-based second-brain and agent operating system."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "Open Scaffold" }
]
dependencies = []

[tool.setuptools]
py-modules = []
